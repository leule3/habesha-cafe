# ---------------------------------------------------------------------
# Habesha Cafe server configuration
# Copy this file to ".env" and fill in your real values. Secrets in a
# .env file must never be committed to git (add .env to .gitignore).
# ---------------------------------------------------------------------

# Port the server listens on (defaults to 3000).
PORT=3000

# MongoDB Atlas connection string (create a free cluster at
# https://www.mongodb.com/atlas, then copy the "Connect your application"
# Driver URI and replace <username>, <password> and <dbname>).
MONGO_URI=mongodb+srv://<db_username>:OAPnVy935nLdNPjw@cluster0.st7sznn.mongodb.net/?appName=Cluster0/habesha-cafe